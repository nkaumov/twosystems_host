## 2026-02-20

### live-interactive
- Добавлена базовая схема БД в `apps/live-interactive/db/schema.sql`.
- Добавлена первая миграция `apps/live-interactive/db/migrations/001_init_live_interactive.sql`.
- Удалена шаблонная миграция `apps/live-interactive/db/migrations/000_example_delete_this_file.sql`.
- Добавлена миграция `apps/live-interactive/db/migrations/002_auth_sessions_seed_user.sql` с таблицей `auth_sessions` и первым тестовым пользователем.
- Добавлен базовый каркас backend v1: модули auth/session/presentations/realtime.
- Добавлены базовые страницы frontend v1: авторизация, список презентаций, каркас редактора.
- Добавлены инструменты работы с БД: `db:migrate`, `db:status`, `db:query`.
- Улучшен вывод `db:query` для SQL с несколькими result set.
- Добавлена поддержка логотипа и вывод на странице входа.
- Интерфейс и API-ошибки переведены на русский язык.
- Добавлена миграция `apps/live-interactive/db/migrations/003_localization_and_second_user.sql` для русификации ролей и добавления второго пользователя.
- Добавлен стабильный роут `/public` для статики, путь логотипа переведен на `/public/media/logo.png`.
- Авторизация переведена на формат `логин + пароль` без обязательного email-формата.
- Добавлена миграция `apps/live-interactive/db/migrations/004_simple_logins_and_flat_access.sql` с логинами `admin` и `manager`, роли отключены.
- Экран презентаций переработан: создание через кнопку и модальное окно.
- Действия в интерфейсе частично переведены на кнопки с emoji-иконками.
- Добавлен переиспользуемый модуль шапки после авторизации: `frontend/scripts/shared/app-header.js` + `frontend/styles/header.css`.
- Кнопка создания перенесена в отдельный блок действий под шапкой.
- Кнопка назад перенесена в отдельный блок навигации под шапкой редактора.
- Обновлена иконка выхода в шапке.
- В строках списка мероприятий кнопка открытия редактора выровнена справа.
- Добавлено полное удаление мероприятия с подтверждением в попапе и API `DELETE /api/presentations/:id`.
- Путь логотипа переведен на `/media/logo.png` с новой версией кеша.
- Исправлен сценарий входа: при 401 теперь показывается явная ошибка вместо «тихого» редиректа.
- Добавлена обратная совместимость входа: фронтенд отправляет и `login`, и `email` для старых версий сервера.
- Добавлена совместимость логинов `admin` и `admin@live.local` (аналогично для `manager`) на backend.
- Для логотипа добавлен fallback по путям `/public/media/logo.png` -> `/media/logo.png`.
- Добавлен прямой публичный роут логотипа `/logo.png` и переведены страницы на него.
- Увеличен и визуально выделен логотип на странице авторизации.
- Логотип продублирован в `frontend/styles/logo.png` и подключен через стабильный путь `/styles/logo.png`.
- Добавлен anti-cache для скрипта логина (`/scripts/login/index.js?v=2`) и его импорта API.
- Убраны инлайн-обработчики `onerror` из HTML/шапки для более чистой разметки.
- Интерфейсные кнопки переведены с emoji на Material Icons.
- Путь логотипа унифицирован: страница входа и шапка используют `/logo.png?v=2`.
- Добавлена команда `npm run build` с проверкой синтаксиса JS (`apps/live-interactive/scripts/build-check.js`).
- Проверена работоспособность: `db:status`, авторизация `admin/admin12345`, загрузка логотипа по путям `/logo.png`, `/media/logo.png`, `/styles/logo.png`.
- Редактор презентации переработан под рабочий макет: список блоков слева, панель настроек по центру и выезжающий предпросмотр справа.
- Добавлено меню типов блоков по кнопке `+` и модальное добавление блока с названием и описанием.
- Добавлен выбор активного блока, визуальное выделение, live-обновление полей настроек и drag&drop сортировка блока в списке.
- Добавлен переключатель в режим `Лайв` с отдельными окнами предпросмотра `ТВ` и `Гость`.
- Состояние блоков редактора сохраняется локально в браузере (черновик по `presentationId`).
- JS редактора разделен на модули: `block-types.js`, `state.js`, `renderer.js`, `index.js`.
- Исправлено поведение попапов/меню редактора: добавлено общее правило `[hidden] { display: none !important; }` в `base.css`.
- Исправлено открытие предпросмотра в редакторе: левая часть (список блоков и настройки) больше не сдвигается при переключении `Лайв`.
- Обновлена анимация переключения `Редактор/Лайв`: экраны теперь переключаются слайдом (редактор уезжает влево, предпросмотр приезжает справа, и обратно).
- В предпросмотре заданы пропорции экранов под реальные устройства: `ТВ` (16:9) и `Гость` (9:19.5).
- Увеличен визуальный размер окна `Гость` в предпросмотре (пропорции телефона сохранены).
- Для `Гибкого` блока добавлены отдельные настройки с раскрывающимися секциями: `Основные` и `Содержимое`.
- В `Содержимое` добавлены поля заголовка и основного текста: текст, цвет, размер (px) и позиция на экране.
- Настройки `Гибкого` блока сохранены в состоянии редактора и локальном черновике (`localStorage`).
- Предпросмотр `Гибкого` блока (`ТВ`/`Гость`) теперь масштабирует размер текста относительно целевого устройства, чтобы px выглядели реалистично.
- Добавлен пересчет масштаба предпросмотра при изменении размера окна браузера.
- В `Гибком` блоке поле `h2` заменено на `p`; в UI раздела `Содержимое` переименовано в `Заголовок` и `Основной текст`.
- Исправлен баг выбора позиции текста: секция `Содержимое` больше не закрывается при клике по позиции.
- Для позиционирования текста возвращены все 9 точек (3x3) и добавлена drag-панель: положение выбирается перетаскиванием мышью.
- Добавлены переиспользуемые motion-утилиты: `frontend/scripts/shared/motion.js` (show/hide/FLIP) и `frontend/styles/motion.css` (глобальные motion-переменные и классы).
- Плашка выбора типа блока теперь открывается/закрывается плавно через motion-слой.
- Drag&drop сортировки блоков переработан на FLIP-анимацию: элементы плавно «раздвигаются» при перетаскивании.
- Исправлена проблема переноса блока вниз: теперь блок можно поставить в самое последнее место (как и в любое другое).
- Аккордеоны настроек сделаны с плавным раскрытием/схлопыванием; одновременно открыт только один раздел.
- Маркер на drag-панели позиционирования текста получил плавное движение и визуальное переливание.
- Исправлен баг в `Внешний вид`: смена типа фона больше не закрывает раздел и не переключает фокус на `Основные`.
- В `Содержимое` добавлена вкладка `Медиа`: загрузка фото/gif, очистка файла, размер и позиция с тем же drag-позиционером.
- Добавлен раздел `Дополнительно`: чекбокс `Показать qr-code подключения`, а также размер и позиция QR-плейсхолдера.
- В предпросмотр `Гибкого` блока добавлены рендер медиа и QR-плейсхолдера с настройками размера/позиции.
- Повторяющиеся контролы (размер + позиция) вынесены в общий генератор в рендерере для переиспользования.
- Усилено визуальное разделение секций настроек: добавлены мягкие фоновые подложки внутри аккордеонов.
- Добавлен новый раздел `Внешний вид` для `Гибкого` блока: тип фона (`Сплошной`, `Градиент`, `Круговой`), основной и второй цвет.
- Предпросмотр `Гибкого` блока теперь учитывает выбранный тип и цвета фона.

## 2026-02-21

### live-interactive
- Исправлено поведение раздела `Внешний вид`: при смене типа фона раздел не закрывается и не переключает интерфейс на `Основные`.
- Уточнена настройка QR: текст изменен на `Показать qr-code подключения (только для ТВ)`.
- Исправлен предпросмотр QR: плейсхолдер QR отображается только в окне `ТВ`.
- Для `Содержимого` добавлена вкладка `Медиа` с загрузкой фото/gif, очисткой файла, размером и позиционированием.
- Добавлен раздел `Дополнительно` с настройкой показа QR, размера и позиции.
- Повторяемые настройки `Размер + Позиция` унифицированы и переиспользуются в разных секциях `Гибкого` блока.
- Чейнджлог переведен на формат с отдельными секциями по дням.
- В `Внешний вид` добавлен новый тип фона `Медиа (растянуть)` с загрузкой фото/gif и растяжением изображения на весь фон экрана.
- Загрузка файлов унифицирована: один обработчик применяется для медиа в содержимом и фонового медиа.
- Уточнено хранение медиа на текущем этапе: загруженные файлы сохраняются в локальном черновике браузера (`localStorage`) как Data URL.
- В `Основные` добавлен выбор показа слайда: `Только ТВ` или `ТВ и гость`.
- Для режима `Только ТВ` окно `Гость` в предпросмотре показывает сообщение: `Дождитесь когда администратор включит следующий слайд.`
- Зафиксирована текущая рабочая точка перед рефакторингом редактора (контрольный прогон `npm run build`).
- Вынесены общие утилиты позиционирования в `frontend/scripts/editor/positioning.js`; дубли в `index.js` и `renderer.js` удалены.
- `frontend/scripts/editor/renderer.js` разделен на модули: `renderers/common.js`, `renderers/flex-settings.js`, `renderers/flex-preview.js`.
- `frontend/scripts/editor/index.js` разделен на контроллеры: `controllers/bootstrap-controller.js`, `controllers/add-block-controller.js`, `controllers/blocks-controller.js`, `controllers/settings-controller.js`, `controllers/preview-controller.js`.
- Усилен безопасный рендер шапки: в `frontend/scripts/shared/app-header.js` убран `innerHTML`, разметка строится через DOM API.
- Удален неиспользуемый метод `reorderBlocks` из `frontend/scripts/editor/state.js`.
- После рефакторинга повторно проверена работоспособность: `npm run build` проходит успешно.
- Для блока `Викторина` добавлены разделы настроек `Внешний вид` и `Дополнительно` (аналогично `Гибкому` блоку).
- Для блока `Викторина` добавлен отдельный раздел `Содержимое` с пустым плейсхолдером под будущие специфичные настройки.
- Вынесены общие контролы визуальных настроек в `frontend/scripts/editor/renderers/visual-shared.js` и переиспользованы в `Гибком` и `Викторине`.
- Добавлена отдельная модель настроек `Викторины` (`frontend/scripts/editor/quiz-block.js`) с дефолтами и нормализацией.
- В `Содержимом` блока `Викторина` реализован стартовый экран: `Создать викторину` и `Импортировать файл викторины из AIVO`.
- Добавлен попап создания викторины; после создания сохраняется JSON-структура викторины в настройках блока.
- Импорт из `AIVO` добавлен как заглушка с информационным сообщением (без загрузки файла на этом этапе).
- Реализован редактор вопросов викторины: тип вопроса (`Выбор из 4 вариантов` / `Текстовый ответ`), текст вопроса, варианты ответа, правильный ответ, удаление вопроса.
- Кнопка `Добавить вопрос` работает по сценарию: до первого вопроса доступна в пустом состоянии, после добавления остается внизу списка вопросов.
- Добавлено закрытие попапа создания викторины по `Esc`.
- Обновлены стили редактора для карточек вопросов и элементов конструктора викторины в `frontend/styles/editor.css`.
- Исправлено отображение попапа создания викторины: модалка вынесена в глобальный слой и больше не обрезается внутри секции аккордеона.
- Исправлена кнопка `Создать` в попапе викторины: добавлена обработка кликов в глобальной модалке и корректное чтение поля названия.
- Редактирование вопросов викторины перенесено из раздела `Содержимое` в отдельный попап `Открыть викторину`.
- В `Содержимом` после создания викторины теперь показывается карточка-сводка (название и количество вопросов) + кнопка `Открыть викторину`.
- Добавлен отдельный глобальный попап редактора викторины с карточками вопросов и кнопкой `Добавить вопрос`.
- Обработчики полей вопросов (`тип`, `текст`, `варианты`, `правильный ответ`) переведены на работу внутри попапа редактора викторины.
- Импорт викторины из `AIVO` переведен с заглушки на рабочий сценарий: выбор `.json`, чтение и нормализация вопросов в структуру блока `Викторина`.
- Добавлена совместимость импорта с форматом брифа `event_manager` (`answers.quizQuestions`), включая типы `SINGLE_CHOICE` и `TEXT`.
- В карточке уже созданной викторины добавлены действия `Импортировать из AIVO` и `Очистить викторину`.
- Для `Очистить викторину` добавлено подтверждение перед удалением всех вопросов.
- Из БД `event_manager` выгружен пример файла импорта: `apps/live-interactive/imports/aivo_quiz_event2_scenario2.json` (на основе брифа мероприятия с викториной).
- Для импорта викторины добавлена поддержка расширения `.aivo` (наряду с `.json`).
- Реализован контракт контейнера `AIVO_QUIZ v1`: поля `format`, `version`, `source`, `quiz.title`, `quiz.questions`.
- Для файлов `.aivo` добавлена строгая валидация происхождения и версии: `source=AIVO`, `token=AIVO_EXPORT_V1`, `version=1`.
- Сгенерирован тестовый файл нового формата: `apps/live-interactive/imports/aivo_quiz_event2_scenario2.aivo`.
- Импорт `.aivo/.json` сделан устойчивым к разным кодировкам (`utf-8`, `windows-1251`, `utf-16le`, `utf-16be`).
- Добавлен auto-fix для частой ручной ошибки в `.aivo`: некавыченный `quiz.title`.
- Исправлен тестовый файл импорта `apps/live-interactive/imports/aivo_quiz_event2_scenario2.aivo`: восстановлена валидная JSON-структура.
- Для очистки викторины `window.confirm` заменен на собственный модальный попап подтверждения (единый стиль интерфейса).
- В контроллере настроек викторины исправлены поврежденные русские строки (`Р...С...`) в текстах ошибок и уведомлений.
- Для блока `Викторина` в разделе `Основные` добавлен выбор показа слайда: `Только ТВ` / `ТВ и гость`.
- Для `Викторины` реализован полноценный рендер в предпросмотре (`ТВ` и `Гость`) с учетом `Внешнего вида` и `Дополнительно`.
- Добавлен выбор вопроса для предпросмотра (`content.previewQuestionIndex`) и переключение вопроса из попапа редактора (иконка `visibility`).
- В предпросмотре `Викторины` для режима `Только ТВ` гость видит экран ожидания, как у `Гибкого` блока.
- Расширена модель настроек `Викторины`: добавлены `main.displayTarget` и `content.previewQuestionIndex` с нормализацией.
- Убран селект выбора вопроса из настроек `Содержимое`; перелистывание вопросов перенесено в сам предпросмотр (`назад/вперед`).
- В предпросмотре викторины добавлены кнопки навигации по вопросам (`chevron_left` / `chevron_right`) с блокировкой на границах.
- Кнопки `назад/вперед` вынесены из внутренних окон (`ТВ`/`Гость`) в общий блок навигации над обоими окнами предпросмотра.
- Исправлена верстка навигации викторины в предпросмотре: блок кнопок перемещен в нижнюю строку под окнами, без влияния на сетку экранов.
- В блок `Викторина` добавлена настройка `Дополнительно -> Страница для подключения` с демо-счетчиком подключенных участников.
- При включении страницы подключения она становится первым слайдом викторины в предпросмотре (QR + счетчик подключенных).
- Навигация предпросмотра (`назад/вперед`) обновлена: учитывает не только вопросы, но и страницу подключения.
- Улучшен показ правильных ответов в викторине: правильный вариант явно помечается бейджем `Верный` на экранах.
- Для текстовых вопросов правильный ответ теперь выводится в предпросмотре и на `ТВ`, и на `Гость`.
- По требованию убран показ правильных ответов в предпросмотре викторины (не подсвечиваются и не выводятся текстом).
- Для страницы подключения изменен экран гостя: вместо QR и счетчика показывается только сообщение `Скоро начнется викторина`.
- Для страницы подключения счетчик на предпросмотре фиксирован как `N` (без ручного ввода количества).
- В `Дополнительно -> Страница для подключения` добавлена отдельная настройка размера QR-code.
- Для `Викторины` в разделе `Внешний вид` добавлены настройки отображения ответов гостей:
  - для вопросов с выбором (`Диаграмма` / `Список ответов` / `Не выводить`);
  - для текстовых ответов (`Лента` / `Карточки` / `Не выводить`).
- Настройки отображения ответов показываются только в режиме `ТВ и гость` (интерактив) и скрываются в режиме `Только ТВ` (эфир/трансляция).
- Системное имя в интерфейсе переименовано в `LUNA`: обновлены заголовки страниц входа/презентаций/редактора и название в шапке после авторизации.
- Для `Викторины` в предпросмотре добавлен сценарий `ТВ и гость + Диаграмма`:
  - на экране `ТВ` показывается вопрос и гистограмма ответов (демо-данные в предпросмотре);
  - на экране `Гость` показывается вопрос и варианты ответа для выбора.
- В настройке `Вопросы с выбором` добавлен режим `Круговая диаграмма`; режимы `Список ответов` и `Не выводить` удалены.
- Для режима `ТВ и гость + Круговая диаграмма` в предпросмотре на `ТВ` показываются:
  - вопрос и круговая диаграмма распределения ответов (демо-данные);
  - ниже список голосов по вариантам.
- Для `Гостя` в этом режиме сохраняется экран вопроса с вариантами ответа.
- Обновлен визуальный стиль круговой диаграммы под референс: кольцо с суммой голосов в центре и строки вариантов с прогресс-барами/числом/процентом под диаграммой.
- Для текстовых вопросов добавлен режим `Облако ответов` в настройке `Текстовые ответы`.
- В режиме `ТВ и гость + Облако ответов` на `ТВ` предпросмотр показывает облако ответов (чипы `ответ • гость`) в стиле референса; на `Госте` остается экран ввода ответа.
- Для текстовых ответов добавлен режим `Куча` (наложенные чипы ответов) в сценарии `ТВ и гость`.
- Из выбора `Текстовые ответы` удалены режимы `Лента`, `Карточки` и `Не выводить`; оставлены только `Облако ответов` и `Куча`.
- Модель `textAnswersView` обновлена: новые допустимые значения `cloud`/`pile`, старые значения автоматически нормализуются в текущий дефолт.
- Запущен MVP live-контура:
  - в редактор добавлена кнопка `Запустить live`;
  - старт live создает новую сессию, временные ссылки `ТВ`/`Гость` и переносит текущие блоки редактора в `session_blocks`;
  - после запуска автоматически открывается новая вкладка управления live.
- Добавлены API live-управления:
  - `POST /api/live/presentations/:id/start`;
  - `GET /api/live/sessions/:id`;
  - `POST /api/live/sessions/:id/current-block`;
  - `POST /api/live/sessions/:id/stop`.
- Добавлен публичный API для экранов по временным токенам: `GET /api/live-public/sessions/by-token/:token`.
- Добавлены страницы live:
  - `/live/manage/:sessionId` (управление, ссылки, переключение эфирного блока, остановка live);
  - `/live/tv/:token` и `/live/guest/:token` (рендер текущего эфирного блока).
- Временные ссылки live автоматически становятся неактивными после остановки сессии.
- Добавлена подготовка к запуску через домен/ngrok:
  - новые env-параметры `PUBLIC_BASE_URL` и `TRUST_PROXY`;
  - формирование live-ссылок теперь учитывает `PUBLIC_BASE_URL` (если задан);
  - при `TRUST_PROXY=true` backend доверяет proxy-заголовкам для корректного протокола.
- Редактор блоков переведен на хранение в БД (`presentation_blocks`) вместо единственного источника в `localStorage`.
- Добавлены API для блоков презентации:
  - `GET /api/presentations/:id/blocks` — загрузка блоков из БД;
  - `PUT /api/presentations/:id/blocks` — полная запись текущего набора блоков в БД.
- Добавлено автосохранение блоков редактора в БД (debounce), чтобы данные были доступны с любого устройства.
- Добавлена автоматическая миграция локального черновика в БД:
  - если у презентации в БД блоков нет, а в `localStorage` они есть, черновик автоматически загружается и отправляется в БД.
- В страницу управления live добавлен встроенный монитор:
  - экран `Что показывает ТВ`;
  - экран `Что видит гость`.
- Блок, выбранный в live как `В эфире`, отображается в мониторе управления с теми же настройками рендера.
- Добавлена синхронизация активного эфира с редактором:
  - при сохранении блоков в редакторе (`PUT /api/presentations/:id/blocks`) активные `live_sessions` этой презентации автоматически получают обновленные `session_blocks`;
  - это позволяет редактировать блоки во время эфира и видеть изменения на live-экранах.
- Для `Викторины` в live-режиме подключен runtime-контекст сессии:
  - QR на странице подключения и QR внутри слайда теперь строятся из реальной гостевой ссылки текущей live-сессии;
  - счетчик подключенных на ТВ странице подключения в live теперь показывает фактическое значение из `session_guests`.
- В public live-экраны добавлен heartbeat гостя (`POST /api/live-public/sessions/by-token/:token/ping`) для актуального подсчета подключенных.
- В live-управлении добавлены/уточнены кнопки переключения слайдов активного многостраничного блока (`prev/next` через API `POST /api/live/sessions/:id/active-block/slide`).
- Рендер `Викторины` адаптирован для live:
  - скрыт служебный заголовок `Страница подключения` в эфирном отображении;
  - убраны лишние декоративные рамки в live-подаче (включая страницу подключения).
- Экран управления live упрощен: список блоков остается в левой узкой колонке, справа — только подписи и экраны `ТВ/Гость` без лишних контейнеров.
- QR в live переведен на локальную генерацию через backend (`GET /api/live-public/qr`) вместо внешнего сервиса.
- Для `Гибкого` и `Викторины` live-рендер QR теперь использует локальный endpoint, что устраняет пустой QR на ТВ при блокировке внешнего API.
- Добавлена зависимость `qrcode` для генерации SVG QR на сервере.
- Для перелистывания слайдов live добавлены совместимые alias-маршруты:
  - `POST /api/live/sessions/:id/slide`
  - `POST /api/live/sessions/:id/slides/shift`
  - основной `POST /api/live/sessions/:id/active-block/slide` сохранен.
- В `live/manage` переключение слайдов сделано с fallback по нескольким маршрутам, чтобы не падать на старых кэшах фронтенда.
- Исправлено автосохранение редактора: сохранение блоков в БД больше не отключается навсегда после первой 404; синхронизация с live продолжает пытаться отправлять обновления.
- Добавлен anti-cache для страниц редактора и live (`editor.html`, `live-manage.html`, `live-tv.html`, `live-guest.html`) через версию скриптов `?v=8`.
- По решению архитектуры отключена динамическая синхронизация активного эфира с редактором:
  - изменения блоков после старта live больше не переносятся в текущую сессию;
  - новые настройки применяются только после перезапуска live.
- `renderers/quiz-preview.js` переписан для стабильного live-рендера:
  - устранены проблемы верстки экранов `ТВ/Гость` в блоке `Викторина`;
  - в live убраны демо-статистики и демо-ответы (до реальных данных показываются нулевые/пустые состояния);
  - сохранен демо-режим только для редакторного предпросмотра.
- Улучшена надежность QR в эфире:
  - основной источник: локальный `/api/live-public/qr`;
  - fallback: внешний генератор `api.qrserver.com`;
  - если оба недоступны — показывается иконка-заглушка QR.

## 2026-02-24

### live-interactive
- Запущена перестройка: из редактора удалена кнопка `Запустить live` и вся клиентская логика старта live-сессий.
- Из backend удалены live-роуты и page-роуты live (`/api/live`, `/api/live-public`, `/live/manage/:sessionId`, `/live/tv/:token`, `/live/guest/:token`).
- Удалены старые файлы live-модуля backend (`backend/src/modules/live/*`).
- Удалены страницы и скрипты live-фронтенда (`frontend/pages/live-*.html`, `frontend/scripts/live/*`, `frontend/styles/live.css`).
- Генерация QR вынесена в нейтральный endpoint `GET /api/qr` (`backend/src/modules/qr/qr.routes.js`) для использования вне live-контура.
- Рендереры предпросмотра переведены с `/api/live-public/qr` на `/api/qr`.
- Заполнен корневой `.gitignore` под структуру монорепозитория (node_modules, сборки, логи, секреты `.env*`).
- Добавлены шаблоны переменных окружения для коммита: `apps/live-interactive/.env.example` и `apps/host-assistant/.env.example`.
